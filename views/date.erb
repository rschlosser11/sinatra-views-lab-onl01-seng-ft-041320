<!DOCTYPE html>
<html>
  <head>
    <title>Date</title>
  </head>
  <body>
    <h1>Today</h1>
    <% require 'date' %>
    <% date = DateTime.now %>
    <p><%= date.strftime('The date is %A, %B %d, %Y') %></p>
  </body>
</html>
